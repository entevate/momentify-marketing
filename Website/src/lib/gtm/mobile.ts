/**
 * Height of the GTM shell's sticky mobile app bar (src/app/gtm/layout.tsx:
 * 10px vertical padding ×2 + the 34px menu button). Anything else that pins
 * to the top on mobile must offset by this or it scrolls under the bar.
 * Keep in sync with that bar. The canonical-UI branch uses the same name.
 */
export const MOBILE_BAR_H = 54
